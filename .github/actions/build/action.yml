name: Build
description: Builds the project based on the detected technology stack.

inputs:
  stack:
    description: The detected technology stack (e.g., java-maven, node-npm)
    required: true

runs:
  using: composite
  steps:
    - name: build
      shell: bash
      run: ${{ github.action_path }}/scripts/build.sh
      env:
        STACK: ${{ inputs.stack }}